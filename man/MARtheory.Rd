% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MARtheory.R
\name{MARtheory}
\alias{MARtheory}
\title{Predict the mutations-area relationship (MAR) from a site frequency spectrum}
\usage{
MARtheory(gm, maxind = NULL)
}
\arguments{
\item{gm}{A \code{\link[=genomaps]{genomaps()}} object.}

\item{maxind}{Maximum number of individuals to predict up to. Default to all
individuals available in genomaps()}
}
\value{
A data frame with class \code{martheory}, with a column \code{N} (number of
individuals) and columns \code{M}, \code{thetaw}, and \code{thetapi}.
}
\description{
\code{MARtheory()} is the theoretical counterpart to \code{\link[=MARsampling]{MARsampling()}}. Instead of
repeatedly sub-sampling the map, it computes the \emph{expected} accumulation of
mutations (and Watterson's theta / nucleotide diversity) with sample size
directly from the site frequency spectrum, under the assumptions that
individuals are randomly distributed in space and that mutations are unlinked.
}
\details{
The returned data frame shares the \code{N}, \code{M}, \code{thetaw} and \code{thetapi} columns
of the \code{\link[=MARsampling]{MARsampling()}} / \code{\link[=MARextinction]{MARextinction()}} output, so it can be overlaid on
the empirical sampling points, or passed to \code{\link[=MARcalc]{MARcalc()}} with \code{Atype = "N"} to
fit the power-law exponent against sample size.
}
\examples{
# Predicted MAR for the 1001 genomes example, overlaid on sampling
expmardf <- MARtheory(gm1001g)
mardf <- MARsampling(gm1001g, nrep = 20)
plot(M ~ N, data = mardf)
lines(M ~ N, data = expmardf, col = "blue")
# fit the predicted power-law exponent against sample size
MARcalc(expmardf, Mtype = "M", Atype = "N")
}
